version: '3.9'

services:
  next-app:
    build: .
    container_name: next-app
    ports:
      - '3000:3000'
    volumes:
      - .:/app
      - next_node_modules:/app/node_modules
    env_file:
      - .env
volumes:
  next_node_modules:


