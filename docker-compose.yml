version: '3.9'

services:
  next-app:
    build: .
    container_name: next-app
    ports:
      - '3000:3000'
    volumes:
      - .:/app
      - next_node_modules:/app/node_modules
    environment:
      DATABASE_URL: mysql://3JR1qiBTyhdjzxk.root:AfDxdw4Ldfxrsv0J@gateway01.ap-southeast-1.prod.alicloud.tidbcloud.com:4000/test?sslaccept=accept_invalid_certs
      NEXTAUTH_SECRET: a1b2c3d4e5f6
      ADMIN_EMAIL: troeunratana@gmail.com
      ADMIN_PASSWORD: ratana2002005pscp
      ADMIN_NAME: RATANA
    depends_on:
      - mysql

  mysql:
    image: mysql:8.4
    container_name: next_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: nextdb
      MYSQL_USER: nextuser
      MYSQL_PASSWORD: password123
    ports:
      - '3306:3306'
    volumes:
      - mysql_data:/var/lib/mysql

# 🔑 បន្ថែម៖ ប្រកាសឈ្មោះ Volume ថ្មីនៅខាងក្រោមគេបង្អស់
volumes:
  mysql_data:
  next_node_modules:


